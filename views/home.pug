extends layouts/main
include mixins/videoBlock

block content
    .videos
        each item in videos
            +videoBlock({
                titie: item.title,
                views: item.views,
                videoFile: item.videoFile
            })