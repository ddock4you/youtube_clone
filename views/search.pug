extends layouts/main
include mixins/videoBlock

block content
    h3 Searching By #{searchingBy}
    if videos.length === 0
        h5 No Video found
    each item in videos
        +videoBlock({
                title:item.title,
                views:item.views,
                videoFile: item.fileUrl,
                id: item.id
            })